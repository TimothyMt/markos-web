# Slice P0.1 — Strategy Spine — intake + lưu trữ

> **Mục tiêu:** cho user khai **xương sống chiến lược** (lõi TẦNG 0 nuôi cả 6 miền CMO): ① Objective đo được **bằng SỐ** · ② Audience/ICP · ③ **Positioning (cấu trúc QUAN HỆ)** · ④ Capacity (**ràng buộc**, không phải trụ co-equal).
> Slice này CHỈ intake + lưu, chưa bơm vào prompt (đó là P0.2).
> **Đọc trước:** `docs/cmo/00-PLAN.md` mục "Kiến trúc 2 tầng × 6 miền" + "7 NGUYÊN TẮC" (nguyên tắc 2,3,4,7 áp thẳng).

## Vì sao cấu trúc này (đã validate 2026-07, không phán solo)
- **Positioning KHÔNG phải 1 câu tự do.** Theo Dunford, định vị là **quan hệ**: bắt đầu từ *khách làm gì nếu không có bạn* (lựa chọn thay thế) → *bạn có gì mà cái đó không có* (khác biệt). Thiếu khung này, định vị "nghe hay nội bộ nhưng vô dụng với khách vì không khác biệt" ([nguồn](https://www.aprildunford.com/post/a-quickstart-guide-to-positioning)). → lưu tối thiểu `alternative` + `differentiator`, không phải 1 chuỗi.
- **Metric/target/baseline là SỐ** thì D6 (đo-học) mới so gap-vs-target.
- **Capacity = ràng buộc** để miền sau **cắt phạm vi** (số kênh D3, độ dài journey D4). Giữ nguyên key `constraint`.
- **`stage` = producer cho recall.** `brain.select(stage=...)` (K2 đã dựng) cần `stage` nhưng **chưa ai đẻ ra** (lỗ hổng `stage ❌` trong WIRING). Spine là nơi khai. **A1 gộp vào đây:** hỏi thẳng 1 câu, **người khai** (enum canonical `launch|growth|scale` — chốt 2026-07). KHÔNG suy tự động ở slice này (lớp Max-tinh-chỉnh-theo-tín-hiệu = **A2** tương lai, dính luật derived-state, để dành khi đã có tín hiệu baseline/lịch sử).

## Dữ liệu — key mới trong `intake_extra` (KHÔNG đổi schema)
```
intake_extra.spine = {
  "stage": str,               # ★ A1 — giai đoạn trưởng thành, người khai: "launch"|"growth"|"scale"|"" (feeds brain.select)
  "objective": {
    "outcome":  str,          # kết quả muốn (vd "tăng đơn online", "gom lead tư vấn")
    "metric":   str,          # nhãn chỉ số (vd "số đơn", "lead", "doanh thu")
    "target":   { "value": number|null, "unit": str, "period": str },  # đích (vd 300, "đơn", "tháng")
    "baseline": { "value": number|null, "unit": str, "period": str },  # hiện tại (vd 120, "đơn", "tháng")
    "deadline": str           # mốc (vd "trước Tết 2026")
  },
  "audience": {
    "who":   str,             # tệp chính / ICP (vd "mẹ bỉm 25-35, TP.HCM")
    "pain":  str,             # nỗi đau / nhu cầu cốt lõi
    "where": str              # họ hay ở đâu (gợi ý kênh)
  },
  "positioning": {            # ★ cấu trúc quan hệ (Dunford), KHÔNG còn là 1 chuỗi
    "alternative":   str,     # khách làm gì nếu không có bạn (đối thủ / cách thay thế)
    "differentiator":str,     # bạn có gì mà cái đó KHÔNG có
    "statement":     str      # 1 câu định vị tổng (tuỳ chọn — user tự viết hoặc để trống)
  },
  "constraint": {             # neo RÀNG BUỘC (lái phạm vi D3/D4), không phải trụ chiến lược
    "people":   str,          # nhân lực (vd "1 người tự làm")
    "budget":   str,          # ngân sách ads (vd "~3tr/tháng")
    "capacity": str           # nhịp làm nổi (vd "3 bài/tuần")
  }
}
```
**Cho phép để trống mọi field** — chuỗi rỗng / `value: null`. `value` khi có PHẢI ép ra số (xem F1 — **locale Việt**). `unit`/`period`/text giữ nguyên (strip + cắt độ dài).

## Cấu trúc trường — chuẩn EVAL
Cấu trúc trên đã qua core-validation (Dunford/STP). Mọi **thêm/bỏ trường tiếp** vẫn phải qua `docs/cmo/EVAL.md` (Test 1 Consumption / Test 2 Answerability / Test 3 Empirical). Ở P0.1 chỉ cần Test 1 + Test 2 (Test 3 cần gen → P0.2). Mọi lệch cấu trúc → ghi lý do theo EVAL trong commit.

## Phân tích mối nối (theo WIRING Hiến pháp)
- Khoá GHI: `intake_extra.spine.*` (objective/audience/positioning **object**/constraint/**stage**). Consumer dự kiến: P0.2 (bơm prompt) · D6 (đo gap) · **`brain.select(stage=...)`** (qua K3). Kiểu: `positioning` = **object** (không phải chuỗi); `target/baseline.value` = number|null; `stage` = **string enum** `launch|growth|scale|""`.
- Khoá `stage` — **đóng lỗ hổng `stage ❌`**: producer = P0.1 (người khai). Khớp enum 2 đầu: giá trị lưu PHẢI ∈ `{launch, growth, scale}` (trùng slug file `brain/stages/*.md` → `select()` lọc trúng). Rỗng `""` = user chưa khai → `select` gọi `stage=None` (bỏ qua chiều stage, không vỡ).
- Derived-state? **Không** ở P0.1 (stage do người khai, `by:"human"`). Lớp Max tự suy/tinh chỉnh = **A2** tương lai (khi đó mới áp log confidence/why/freeze/override).
- Degrade: mọi field để trống được (`""`/`null`); `stage=""` → recall bỏ qua stage; `value=null` → D6 không so gap (êm, không lỗi).
- Addendum (Hiến pháp không phủ): enum `stage` phải **khớp slug** `brain/stages/` — nếu sau này đổi tên stage trong brain thì phải sửa cả FE radio + validate F1 (ghi chú chéo cho A2).

---

## Chẻ theo FUNCTION (mỗi F = 1 phiên Cline = 1 commit = 1 review)
> Theo `docs/cmo/WORKFLOW.md`. Tuần tự F1→F4, mỗi F chờ review PASS mới sang F kế.

### F1 — `save_spine()` (backend ghi) · `webapp/business.py`
> **Lưu ý:** đã có bản F1 nháp (uncommitted) dùng positioning=chuỗi + parse số locale Tây. **Làm lại F1** cho khớp brief đã vá: positioning = **object**, và ép số theo **locale Việt**.
- Grep `save_messaging` / `save_pillars` (KHÔNG đọc cả file) → theo khuôn.
- `async def save_spine(user_id=None, spine: dict = None) -> dict:` — load profile → set `intake_extra["spine"] = {...}` (giữ đúng cấu trúc trên: **stage**/objective/audience/**positioning object**/constraint; strip; cắt độ dài; **ép `target/baseline.value` → số/None**) → store update → `{"ok": True}`.
- **`stage` — validate enum:** chỉ chấp nhận `{"launch","growth","scale"}` (lower+strip); giá trị khác/rỗng → lưu `""`. (Khớp slug `brain/stages/` để `select()` lọc trúng.) Test khô (dán commit): `"launch"→"launch" · "Growth "→"growth" · "abc"→"" · ""→""`.
- **Ép số theo QUY ƯỚC SỐ VIỆT NAM** (`,` = thập phân, `.` = phân nhóm nghìn):
  - Bỏ ký tự không phải chữ số/`.`/`,`/`-`; chỉ lấy cụm số đầu.
  - Có cả `.` và `,`: dấu **cuối cùng** là thập phân, các dấu trước là phân nhóm.
  - Chỉ có `.` → phân nhóm nghìn, bỏ hết (`1.500`→1500, `1.500.000`→1500000).
  - Chỉ có `,` → thập phân (`1,5`→1.5).
  - Parse fail → `None`. Số thật (int/float) → `float`.
  - Test khô bắt buộc (dán vào commit): `1.500→1500.0 · 1.500.000→1500000.0 · 1,5→1.5 · 2,5 tỷ→2.5 · 300 đơn→300.0 · abc→None`.
- Commit: `feat(cmo): P0.1 F1 save_spine (positioning object + số locale VN)`.

### F2 — expose `bizSpine` · `webapp/business.py biz_data()`
- Thêm `bizSpine = intake_extra.get("spine", {})`. Trivial → có thể gộp commit F1; nếu tách: `feat(cmo): P0.1 F2 expose bizSpine`.

### F3 — route API · `webapp/api.py`
- Handler `biz_spine_save`: đọc JSON `{spine:{...}}` → `business.save_spine`. `Route("/api/biz/spine/save", ...)` trong `api_routes()`. Theo khuôn route messaging/pillars.
- Commit: `feat(cmo): P0.1 F3 route /api/biz/spine/save`.

### F4 — FE khối "🎯 Chiến lược nền" · `web/app.js` **+ mirror standalone (CÙNG 1 COMMIT)**
- Khối đầu trang Chiến lược. 5 nhóm ô:
  - **Giai đoạn (`stage`):** 3 nút radio/select — `launch` ("Mới ra mắt — kiếm khách đầu") · `growth` ("Đang tăng — có công thức lặp") · `scale` ("Nhân rộng — hệ thống hoá"). Cho bỏ trống. Nhãn tiếng Việt tự nhiên, value gửi lên = slug enum.
  - **Mục tiêu:** outcome · metric · target (số+đơn vị+kỳ) · baseline (số+đơn vị+kỳ) · deadline.
  - **Tệp khách:** who · pain · where.
  - **Định vị (2 ô + 1 tuỳ chọn):** `alternative` ("Không có bạn, khách làm gì?") · `differentiator` ("Bạn có gì họ không có?") · `statement` (câu định vị, tuỳ chọn).
  - **Năng lực:** people · budget · capacity.
- Nút Lưu → POST `/api/biz/spine/save`; render lại từ `bizSpine`.
- **Mirror y hệt sang `dashboard-standalone.html`** (`<script>` + `<style>`) — **cùng commit**.
- Commit: `feat(cmo): P0.1 F4 FE khối Chiến lược nền + mirror standalone`.

## Không làm ở slice này
- Chưa bơm spine vào prompt (P0.2). Chưa dùng `constraint` cắt kênh/journey (D3/D4). Không thêm cột/bảng DB.

## Verify + Xong
- Mỗi function: chạy verify commands (WORKFLOW/INDEX) + dán self-review report vào commit trước khi push. **Windows dùng `py`, không phải `python3`.**
- Slice P0.1 xong khi F1–F4 đều PASS cổng review.
