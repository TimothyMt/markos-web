# Slice P0.2 — Bơm Strategy Spine vào các prompt (ladder-up)

> **Mục tiêu:** mọi kế hoạch/nội dung **truy ngược về Spine**. Slice này chỉ *tiêm ngữ cảnh spine* vào prompt, không đổi flow.
> **Phụ thuộc:** P0.1 đã lưu `intake_extra.spine`.
> **Đọc trước:** `00-PLAN.md` nguyên tắc 1 (bơm cả 4 neo, không chỉ objective) + 3 (capacity là ràng buộc, không phải bối cảnh).

## Việc chính — 1 helper + gắn vào vài hàm gen
1. Viết helper **`_spine_anchor(extra) -> str`** (nhận `extra`=intake_extra ĐÃ load, **KHỚP khuôn `_messaging_anchor_from(extra)`** ở `business.py:2784` — grep đọc quanh đó, đừng đọc cả file; **KHÔNG** nhận `uid`, KHÔNG load DB lại): đọc `extra.get("spine")`, nếu rỗng/không dict → trả `""`; nếu có → trả block prepend `\n\n# ...` kiểu:
   ```
   # CHIẾN LƯỢC NỀN — mọi đề xuất phải phục vụ khối này
   Giai đoạn: {stage}   ← chỉ in nếu có (launch/growth/scale)
   Mục tiêu: {outcome} — đo bằng {metric}, đích {target.value}{unit}/{period} (hiện {baseline.value}{unit}/{period}), mốc {deadline}
   Tệp khách: {who} — nỗi đau: {pain} — thường ở: {where}
   Định vị: khách thường {alternative}; mình khác ở {differentiator}{ — "statement" nếu có}
   Năng lực (RÀNG BUỘC — ĐỪNG đề xuất vượt quá): {people}, ngân sách {budget}, làm nổi {capacity}
   ```
   - **`positioning` là OBJECT** `{alternative, differentiator, statement}` (P0.1) — format 3 field con như trên, **KHÔNG in thẳng dict**. Nếu chỉ có `statement` → in mỗi statement.
   - **`stage`** = string enum (có thể `""`) — chỉ in dòng Giai đoạn khi khác rỗng.
   - `target/baseline.value` là number|null — bỏ qua khi null. Bỏ mọi field rỗng cho gọn (đừng in "đích /" khi trống).
2. Gắn `_spine_anchor` vào **user prompt** của các hàm sinh kế hoạch/nội dung (thêm vào chuỗi ghép prompt, cạnh `msg_anchor`/`strat_anchor` nếu có). Grep tên hàm để tìm vị trí (số dòng trong brief cũ có thể lệch — **xác minh bằng grep, đừng tin số dòng**):
   - `gen_master_plan`
   - `gen_campaign_portfolio`
   - `campaign_plan`
   - `gen_calendar_post`
   - `calendar_plan` — nếu nó ghép prompt
3. Thêm **1 câu chỉ dẫn** sau anchor:
   > "Ưu tiên đề xuất phục vụ mục tiêu trên, phù hợp tệp khách + định vị, và **không vượt năng lực** đã nêu. Nếu một bài/kế hoạch không phục vụ trực tiếp mục tiêu, nói rõ vai trò gián tiếp."
   **ĐỪNG viết lại prompt gốc** — chỉ CHÈN anchor + câu này.

## ⚠️ Guardrail (business.py ~4300 dòng)
- **TUYỆT ĐỐI KHÔNG Write đè cả file** — chỉ Edit/chèn có mục tiêu. Lần trước Write đè làm mất 3630 dòng. Xong mỗi bước tự chạy `git --no-pager diff --numstat webapp/business.py` — có dòng **xoá** bất thường = SAI, hoàn tác.
- **Xem git: `git --no-pager diff`** (tránh treo pager). KHÔNG `head`/`tail` (PowerShell không có).

## Chẻ theo FUNCTION (mỗi F = 1 commit chờ review)
### F1 — helper `_spine_anchor(extra)` + gắn vào `gen_master_plan`
- Viết `_spine_anchor(extra) -> str` ngay cạnh `_messaging_anchor_from` (business.py ~2784), theo khuôn (positioning=object, stage, bỏ field rỗng — xem trên).
- Gắn vào `gen_master_plan`: hàm build biến `user = (...)` (khoảng dòng 1856-1858, kết bằng `# Chiến lược\n{synth[:1800]}`). Thêm **1 dòng** ngay sau: `user += _spine_anchor(extra)` (`extra` đã load ~1838). Thêm câu chỉ dẫn (mục 3) vào cuối anchor HOẶC nối vào `user`.
- Self-verify: `py -c "import webapp.business as b; print(b._spine_anchor({'spine':{'stage':'launch','objective':{'outcome':'tăng đơn','metric':'đơn','target':{'value':300,'unit':'đơn','period':'tháng'}},'positioning':{'alternative':'mua Highlands','differentiator':'rang xay tại chỗ'},'constraint':{'people':'1 người','capacity':'3 bài/tuần'}}}))"` → in block có Giai đoạn/Mục tiêu/Định vị/Năng lực; `b._spine_anchor({})` → `""`.
- Commit: `feat(cmo): P0.2 F1 _spine_anchor + gắn gen_master_plan`.

### F2 — gắn `_spine_anchor(extra)` vào 4 hàm còn lại
- `gen_campaign_portfolio`, `campaign_plan`, `gen_calendar_post`, `calendar_plan` (nếu ghép prompt string). Grep từng hàm, tìm biến prompt `user`/ghép chuỗi, thêm `_spine_anchor(extra)` (đảm bảo `extra`=intake_extra có trong scope; nếu tên khác như `_pe` thì dùng tên đó). Hàm nào KHÔNG ghép prompt string đơn giản → ghi chú, để Claude quyết, đừng ép.
- Commit: `feat(cmo): P0.2 F2 gắn spine anchor vào 4 hàm gen còn lại`.

## Kiểm thử tay (nêu trong commit) — đây cũng là TEST 3 (empirical) của P0.1
> **Cần API key thật:** set ≥1 env `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` / `GEMINI_API_KEY` (theo `run_web.py`). Dùng env var, ĐỪNG hardcode/commit key.
- Set 1 spine mẫu (có audience + capacity "1 người, 3 bài/tuần") → chạy `gen_master_plan` / `gen_calendar_post`.
- Xác nhận output: (a) bám đích/tệp, (b) **không đề xuất khối lượng vượt capacity** (vd không phịa "đăng 5 kênh × 2 bài/ngày"). So với khi spine rỗng.
- Nếu một trường spine (vd trường mượn từ framework ở P0.1) **không làm output khác gì** → báo Claude: có thể trượt Test 3, cân nhắc bỏ.

## Ranh giới
- KHÔNG đổi chữ ký hàm kiểu phá vỡ; chỉ thêm ghép chuỗi nội bộ.
- Nếu 1 hàm không ghép prompt kiểu string đơn giản → ghi chú lại, để Claude quyết, đừng ép.

## Xong
- Verify → commit: `feat(cmo): P0.2 bơm spine anchor (4 neo) vào prompt kế hoạch/nội dung`.
