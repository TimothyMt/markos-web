# Brief B2.1 — Layered: NỀN matrix (trụ × phễu × nền tảng) + ĐỢT spike (BACK-END)

> **Mục tiêu:** dựng đúng cấu trúc CHAIN-V2 đã chốt — **House → Trụ → Phễu → Nền tảng**, chạy theo mô hình
> **Layered = NỀN matrix (always-on) + ĐỢT spike (time-boxed)**. B2 hiện có `key_ideas[].funnel_map` là
> **danh sách bài PHẲNG mồ-côi** (không có trục trụ, không có nền tảng-repurpose, không tách nền/đợt).
> B2.1 nâng nó thành **ma trận 2 chiều (trụ × phễu) × nền tảng**, và tách rõ 2 lớp: cái chạy đều (nền) vs
> cái đẩy từng đợt (spike). **KHÔNG đập đi làm lại** — thêm trục, giữ tương thích ngược B2 đã merge (#9).
> **Phạm vi B2.1 = BACK-END + nối ống (biz_data/api) ONLY.** Màn hình (Nền matrix + Đợt lens) → brief
> **B2.1-UI** riêng (giống B1/B1-UI, B2/B2-UI). PR #10 (B2-UI cũ) GIỮ, KHÔNG merge — layout sẽ đổi theo B2.1.
>
> **Đọc trước:** `WIRING.md` (Hiến pháp mối nối) · brief **B2** (`B2-key-idea-funnel.md` — data shape gốc) ·
> `webapp/business.py`: `suggest_key_ideas`(~3202) · `save_key_idea`(~3281) · `gen_funnel_map_for_idea`(~3337) ·
> `_playbook_segments`(~3194) · `_KI_TIERS`/`_GOAL_RATIO`(~3178) · `gen_messaging`/`save_messaging`(producer `messaging.pillars`) ·
> `_gen_playbook`(~4228, producer `playbook_struct.struct.segments[].tiers.{tofu,mofu,bofu}[].{territory,huong,channels}`) ·
> `frameworks.industry_context.get_purchase_archetype` (trục nền tảng theo archetype).
> **Branch:** `claude/pb-wire-brief-b1-3iptbf` từ `staging` (đã có B1+B2+PB-A) · PR về `staging` · KHÔNG tự merge.

---

## 🌏 LUẬT ĐA NGÀNH (bất biến — mọi mũi phải qua)
Sản phẩm phục vụ **nhiều ngành**. KHÔNG hardcode ví dụ 1 ngành vào code/prompt/schema.
1. **Trụ + tuyến = data-driven** — mọi trụ sinh từ `messaging.pillars` (T3) + góc đánh `playbook_struct` (T2) của
   *chính doanh nghiệp đó*. KHÔNG nhét territory mẫu ("Khoa học làn da"…) vào code — đó chỉ là minh hoạ 1 ngành.
2. **Nền tảng chọn theo ARCHETYPE, KHÔNG cố định FB/TikTok/IG** — `get_purchase_archetype(industry)`:
   impulse (mua cảm xúc) → nghiêng video ngắn/UGC; trust_building (dịch vụ/cao cấp) → nghiêng bài dài/kênh riêng tư;
   demand_gen → nghiêng giáo dục/so sánh. Trục nền tảng UỐN theo đây + `current_channels` user đang dùng.
3. **Ngành nhạy cảm** (y tế/tài chính/TPCN/mỹ phẩm…) — giữ nguyên luật proof/cấm-claim (B1) + compliance (PR-A).
   B2.1 KHÔNG nới các luật này; ma trận chỉ TỔ CHỨC bài, không sinh câu chữ (đó là B3).
4. **Nhịp nền vs đợt uốn theo ngành** — ngành mùa vụ (F&B/bán lẻ) nghiêng spike; dịch vụ B2B nghiêng nền chạy đều.
   Mô hình Layered phục vụ cả hai: nền = xương sống, đợt = biến thiên.
5. **Ratio/volume mặc định** — ngoài `goal` (B2 đã có), nudge thêm theo archetype; KHÔNG ép 1 khuôn mọi ngành.

## 3 quyết định đã chốt (bám — KHÔNG lệch)
1. **Additive & tương thích ngược** — post cũ `{tier,channel,role,note}` VẪN đọc được; B2.1 THÊM trục
   `pillar` + `sibling_group`, thêm key mới `base_matrix`, thêm field `spike_*` vào key_idea. KHÔNG đổi schema DB,
   KHÔNG xoá khoá cũ, KHÔNG phá `funnel_map` đã lưu (thiếu trục mới → degrade coi như 1 trụ / không repurpose).
2. **Degrade-safe** — thiếu `playbook_struct` → tụt về `messaging.pillars`; thiếu cả pillars → tụt về synthesis/core.
   Không bao giờ trả cụt-im-lặng (đúng lỗi funnel cũ). Mọi trục mới có đường degrade.
3. **Dừng ở KHUNG (tổ chức bài), không nhảy cóc** — B2.1 chỉ dựng ô ma trận + gán trục cho bài dự kiến.
   KHÔNG sinh câu chữ, KHÔNG đổ thẻ calendar (B3). KHÔNG làm FE (→ B2.1-UI).

---

## 🧱 Mô hình dữ liệu (2 lớp — key trong `intake_extra`, KHÔNG đổi schema DB)

### Lớp NỀN — `intake_extra.base_matrix` (MỚI, always-on, độc lập đợt)
Ma trận xương sống: **trụ × phễu**, mỗi ô mang **nền tảng** (repurpose) + nhịp gợi ý. Đây là "cái chạy đều".
```
intake_extra.base_matrix = {
  "cells": [                                  # trụ × phễu — CHỈ ô có nội dung (không ép đủ 100% ô, luật volume)
    {
      "pillar": "<trụ = messaging.pillars[].territory>",   # đa ngành: sinh từ T3, KHÔNG hardcode
      "tier": "tofu|mofu|bofu",                            # phễu chạy TRONG trụ
      "role": "<ô này làm gì trong phễu của trụ — 1 câu>",
      "platforms": ["<nền tảng hợp archetype/current_channels>"],  # 1 ô repurpose sang các nền tảng này
      "cadence": "<nhịp gợi ý: vd '1 bài/2 tuần' | '' = tuỳ chọn>"
    }
  ],
  "updated_at": <ts>
}
```

### Lớp ĐỢT — `intake_extra.key_ideas[]` (nâng B2, thêm trục spike + repurpose)
Đợt = **spike**: đẩy MỘT tầng phễu (hoặc vài trụ) mạnh lên trong 1 kỳ hạn. Field THÊM (additive):
```
key_idea += {
  "spike_tier": "tofu|mofu|bofu|",     # đợt ĐẨY tầng nào across trụ. '' = suy từ goal (B2 đã map goal→ratio)
  "spike_pillars": ["<trụ A>", ...]    # đợt tập trung trụ nào. [] = trải mọi trụ
}
key_idea.funnel_map.posts[] += {       # mỗi bài giờ có toạ độ ma trận + nhóm repurpose
  "pillar": "<trụ — ô nào>",           # '' nếu bài cũ/không xác định trụ (degrade: coi như trụ chung)
  "sibling_group": "<group-id | ''>"   # cùng group = 1 nội dung repurpose sang nhiều nền tảng (nền tảng axis)
}
# post đầy đủ = {pillar, tier, channel, role, note, sibling_group}   — 4 khoá cũ GIỮ NGUYÊN
```

---

## ① `gen_base_matrix` — dựng NỀN matrix (trụ × phễu × nền tảng) (1 commit) — MỚI
**File:** `webapp/business.py` (hàm mới, cạnh `gen_funnel_map_for_idea`).
**ĐỌC:** `messaging.pillars` (trụ) · `playbook_struct` (tier×channels, degrade) · `synthesis` · archetype · `current_channels`.
**GHI:** `intake_extra.base_matrix`.

- Trụ = `messaging.pillars[].{territory,angle}` (đa ngành — producer `save_messaging` ✓). Thiếu pillars → degrade:
  1 "trụ chung" từ `messaging.core`/synthesis. Thiếu cả → `{error}` mềm ("chưa có Thông điệp để dựng nền").
- Với mỗi trụ, xoay qua 3 tầng phễu (tofu/mofu/bofu), lấy **kênh gợi ý** từ `playbook_struct` Hướng khớp trụ
  (`channels`) → degrade → chọn nền tảng theo **archetype** (luật đa ngành #2) giao với `current_channels`.
- 1 LLM call JSON → `{cells:[{pillar,tier,role,platforms,cadence}]}`. **Volume tỉnh táo (luật #5 + handoff 9 bài/tuần
  quá tải team 2-3):** KHÔNG ép đủ mọi ô trụ×tier; chọn ô đắt giá, nhịp thưa hợp nguồn lực.
- **Validate (chống lỗi funnel cũ):** `tier ∈ {tofu,mofu,bofu}` (rác → bỏ ô) · `pillar` phải thuộc danh sách trụ
  đã biết (khớp lỏng, không khớp → gán trụ gần nhất / bỏ) · `platforms` cắt list, rỗng → điền 1 nền tảng archetype ·
  `role` bắt buộc (thiếu → bỏ ô). Trần token cứng, KHÔNG lưu string đứt.
- **Degrade:** cells rỗng sau validate → dựng tối thiểu 1 ô/trụ ở tofu (kênh archetype). KHÔNG trả cụt.
- **Derived-state? KHÔNG** — base_matrix là *gợi ý cấu trúc* non-binding, user sửa/xoá ô = override sẵn trong thiết kế.
- Ghi `base_matrix` + `updated_at`. Trả `{ok, base_matrix}`.

## ② `save_key_idea` — thêm trục spike (đợt = đẩy tầng/trụ nào) (1 commit) — NÂNG
**File:** `webapp/business.py` `save_key_idea`. **ĐỌC/GHI:** `key_ideas` (thêm 2 field).
- Thêm tham số `spike_tier=""`, `spike_pillars=None`. Chuẩn hoá: `spike_tier` về `{tofu,mofu,bofu}` (rác → `""`);
  `spike_pillars` = list string cắt gọn (không phải list → `[]`). Nhét vào `meta` (update GIỮ funnel_map cũ như B2).
- **KHÔNG bắt buộc** — `spike_tier=''` → suy từ `goal` lúc gen (B2 đã map goal→ratio). Con người đặt = thắng.
- `api.py` handler `biz_key_idea_save` truyền 2 field mới qua (đọc từ payload, mặc định rỗng).

## ③ `gen_funnel_map_for_idea` — đợt bám NỀN + gán trục trụ + repurpose nền tảng (1 commit) — NÂNG
**File:** `webapp/business.py` `gen_funnel_map_for_idea`. **ĐỌC thêm:** `base_matrix` (nền làm bối cảnh) + `spike_*`.
**GHI:** `key_ideas[i].funnel_map.posts[]` (thêm `pillar` + `sibling_group`).
- Nạp `base_matrix.cells` vào prompt làm **NỀN** (đợt là spike NGẮN chồng lên nền, không dựng lại từ đầu).
- **Spike bias:** nếu `spike_tier` set → dồn bài vào tầng đó across trụ; nếu `spike_pillars` set → chỉ các trụ đó.
  Rỗng → theo `goal`→ratio (B2, giữ nguyên `_GOAL_RATIO`).
- Prompt yêu cầu mỗi bài mang **`pillar`** (thuộc trụ nào) + gợi ý **`sibling_group`** (bài lõi + biến thể repurpose
  sang nền tảng khác dùng chung group-id — luật đa ngành: chọn nền tảng theo archetype/current_channels).
- **Validate (mở rộng B2, tương thích ngược):** giữ lọc `tier`/`channel`/`role` cũ; THÊM: `pillar` khớp lỏng danh
  sách trụ (không khớp → `''`, KHÔNG bỏ bài); `sibling_group` chuẩn hoá string ngắn (rỗng OK). Bài cũ thiếu 2 trục
  mới → điền `''` (degrade, không vỡ). posts rỗng → degrade tối thiểu như B2 (giữ nguyên).
- Trả `{ok, funnel_map}` (funnel_map giờ posts có thêm 2 khoá).

## ④ Nối ống ra FE
- **`biz_data`** (~311, cạnh `bizKeyIdeas`): thêm `out["bizBaseMatrix"] = extra.get("base_matrix") or {}` (guard dict).
- **`api.py`** `api_routes()`: thêm 1 handler + `Route("/api/biz/base-matrix/gen", ...)` gọi `gen_base_matrix`.
  (2 route B2 cũ giữ; `biz_key_idea_save` chỉ truyền thêm `spike_*`.)

---

## 🔌 Phân tích mối nối (seam — làm JIT tại brief-time, luật WIRING)
| Khoá đọc/ghi | Producer | Khớp? | Degrade nếu thiếu |
|---|---|---|---|
| `base_matrix` (ghi ①, đọc ③/FE) | `gen_base_matrix` (①, MỚI) | tên/kiểu dict thống nhất 3 điểm | đọc `.get('base_matrix') or {}` → cells rỗng → prompt đợt tự dựng |
| `base_matrix.cells[].pillar` | trụ từ `messaging.pillars[].territory` | khớp lỏng chuỗi | pillars rỗng → 1 "trụ chung" từ core/synthesis |
| `base_matrix.cells[].tier` | enum `_KI_TIERS` | khớp enum 2 đầu (↔ tier lịch/đo T6/T7) | rác → bỏ ô |
| `base_matrix.cells[].platforms` | archetype + `current_channels` | list string | rỗng → 1 nền tảng archetype |
| `key_idea.spike_tier` | `save_key_idea` (②) | enum `_KI_TIERS` \| '' | '' → suy từ `goal` (③) |
| `key_idea.spike_pillars` | `save_key_idea` (②) | list ↔ pillar trong base_matrix/messaging | [] → trải mọi trụ |
| `funnel_map.posts[].pillar` | `gen_funnel_map_for_idea` (③) | khớp lỏng trụ; bài cũ → '' | '' → coi như trụ chung (không vỡ) |
| `funnel_map.posts[].sibling_group` | ③ | string tự do, dùng để B3 nhóm repurpose | '' → bài đơn, không repurpose |
| `funnel_map.posts[].{tier,channel,role,note}` | ③ (B2, GIỮ) | enum/str như B2 | như B2 |
- **Tới runtime được?** cả 3 mũi lazy-import deps trong hàm (chuẩn `business.py`); route đăng ký `api_routes()`.
- **Derived-state?** KHÔNG — base_matrix + spike đều non-binding, user override thắng (không cần confidence/why-log).

## Verify (offline — không key/DB)
```bash
python3 -c "import webapp.business as B; assert callable(B.gen_base_matrix) and callable(B.gen_funnel_map_for_idea) and callable(B.save_key_idea); print('funcs OK')"
python3 -c "import webapp.business, webapp.api"      # (sandbox thiếu starlette/anthropic → khai rõ nếu vậy)
python3 tests/test_b2_key_idea.py                    # regression B2 CŨ vẫn PASS (tương thích ngược)
python3 tests/test_b21_layered_matrix.py             # regression MỚI (stub llm_router+storage)
```
Regression `tests/test_b21_layered_matrix.py` (mới) chốt điều dễ vỡ:
- `gen_base_matrix`: validate lọc tier rác + ô thiếu role; pillar khớp trụ messaging; platforms rỗng → điền archetype;
  degrade không playbook_struct → vẫn ra cells từ pillars (KHÔNG cụt).
- `save_key_idea`: `spike_tier` rác → ''; `spike_pillars` không-list → []; update GIỮ funnel_map cũ (không hồi quy B2).
- `gen_funnel_map_for_idea`: posts mang `pillar`+`sibling_group`; **bài cũ thiếu 2 trục → điền '' KHÔNG vỡ** (tương thích
  ngược); spike_tier set → prompt dồn tầng đó; base_matrix nạp vào prompt làm nền.
- Hành vi LLM thật (Max tổ chức ma trận hợp lý, repurpose đúng nền tảng theo ngành) **chờ chạy staging** — khai self-review.

## Self-review report (dán vào commit cuối)
```
[B2.1] Layered — NỀN matrix (trụ×phễu×nền tảng) + ĐỢT spike + repurpose sibling_group
Đã check: gen_base_matrix import OK · base_matrix validate/degrade · save_key_idea +spike_tier/spike_pillars ·
          funnel_map.posts +pillar/+sibling_group TƯƠNG THÍCH NGƯỢC (bài cũ không vỡ) · biz_data +bizBaseMatrix ·
          +1 api route · test_b2 CŨ vẫn PASS · KHÔNG đổi schema DB · KHÔNG hardcode ngành (trụ từ T3, nền tảng từ archetype)
Chưa chắc (chờ runtime): Max tổ chức ma trận có gọn/đúng nguồn lực · repurpose có đúng nền tảng theo archetype ngành → soi staging rồi siết
```

## Không làm
- KHÔNG đập/viết lại `funnel_map` cũ — CHỈ thêm trục, giữ tương thích ngược (bài cũ `{tier,channel,role,note}` vẫn chạy).
- KHÔNG hardcode territory/nền tảng của 1 ngành vào code/prompt (luật đa ngành #1,#2).
- KHÔNG đổ thẻ calendar, KHÔNG sinh câu chữ bài (B3). KHÔNG làm FE (→ B2.1-UI).
- KHÔNG đổi schema DB / schema `messaging` / `playbook_struct`. KHÔNG bịa số/proof (giữ luật cấm bịa).
- KHÔNG nới luật proof/cấm-claim/compliance ngành nhạy cảm (luật đa ngành #3).
- Mỗi mũi 1 commit · push nhánh riêng · PR về `staging` · **dừng chờ review, KHÔNG tự merge.**
