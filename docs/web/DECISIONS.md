# Nhật ký quyết định (ADR-lite) — Web App

> Mỗi quyết định 1 dòng tiêu đề + bối cảnh + lựa chọn + hệ quả. Không xoá, chỉ
> đánh dấu "Đã thay thế bởi D-xxx" khi đổi. Đừng tranh luận lại cái đã chốt.

| ID | Quyết định | Trạng thái |
|----|-----------|-----------|
| D-001 | Web là surface mới, **bot giữ nguyên**; web mount chung server hoặc chạy độc lập | ✅ |
| D-002 | v1 **chưa làm auth** (mock-first, demo). Bắt buộc có auth + RLS trước khi public dữ liệu thật đa user | ⏳ nợ |
| D-003 | Backend **pluggable**: SQLite (dev) ↔ Supabase (prod) qua `store` facade | ✅ |
| D-004 | Dùng **chung Supabase với bot**; web đọc bảng thật qua `business.py`, không nhân bản dữ liệu | ✅ |
| D-005 | AI dùng **1 API key của hệ thống**, tính usage theo **quota từng user**; user "nạp quota" để dùng (chưa nối thanh toán) | ✅ |
| D-006 | Token **Facebook là OAuth riêng từng user**; user tự kết nối để xem ads của họ | ✅ |
| D-007 | Triết lý sản phẩm: **Max (CMO) làm trung tâm**, điều hướng theo **hành trình 5 chặng**, không phải dashboard Ads-manager | ✅ |
| D-008 | **Không UI giả** — nút không có tác dụng phải gỡ (audit 2026-06-16) | ✅ |
| D-009 | Deploy: **Railway** (server đầy đủ) + **GitHub Pages** (bản tĩnh demo) | ✅ |
| D-010 | Realtime qua **SSE** + Supabase Realtime; watcher poll 4s làm fallback | ✅ |
| D-011 | Phục vụ **cả 3 nhóm user, phân theo GÓI** (Starter/Pro/Agency); core dùng chung, gating theo gói | ✅ (2026-06) |
| D-012 | Luồng vàng ưu tiên: **M0 = Chẩn đoán→Chiến lược**, **M1 = Sản xuất nội dung**; Ads để sau | ✅ (2026-06) |
| D-013 | Kinh doanh: **thuê bao tháng** 3 gói; billing + auth gom vào M4 | ✅ (2026-06) |
| D-014 | Auth **hoãn** ở các milestone tới (Q-D); v1 demo/nội bộ. ⚠️ Chặn cứng public dữ liệu thật đa user | ✅ (2026-06) |
| D-015 | Web chạy agent **non-interactive** = `run_targeted_pipeline` (đủ 8 bước, gồm synthesis). Multi-agent + 8 câu hỏi chỉ dùng trong chat bot | ✅ (2026-06) |
| D-016 | M0: Starter **luôn chạy full** chất lượng cao; trang Chiến lược **tóm tắt gọn trước** + nút "Xem đầy đủ" | ✅ (2026-06) |
| D-017 | Lịch nội dung theo mô hình bot: **🟢 Always-on (nền, quanh năm)** + **🔴 Campaign theo dịp (lớp phủ có window)**. Web phải phản ánh 2 track + lifecycle. Thuộc **M1** | ✅ (2026-06) |
| D-018 | **Always-on chạy SONG SONG, KHÔNG tắt** trong tuần có campaign; campaign là lớp CỘNG THÊM (không thay thế). Sai marketing nếu always-on trống khi đang có đợt | ✅ (2026-06) |
| D-019 | **Sinh nội dung inline trong lịch, theo tuần/slot** (nhẹ, đúng ngữ cảnh) thay vì batch cả tháng 1 lần. Trang "Trình tạo nội dung" tách riêng = chế độ batch nâng cao | ✅ (2026-06) |
| D-020 | Lịch có **2 view**: Kế hoạch tháng (Gantt theo tuần) + Chi tiết tuần (ngày) | ✅ (2026-06) |
| D-021 | **Chat ≠ nơi lưu trữ.** Max chat = buồng lái (tạm thời); output Max tạo (skill_runs/strategies) lưu bền ở Supabase, xem lại ở trang chặng + **Hồ sơ doanh nghiệp** | ✅ (2026-06) |
| D-022 | Gộp 2 trang trùng (pipeline + agents) → **1 trang `dossier` "Hồ sơ doanh nghiệp"** (tủ hồ sơ bền vững). `#pipeline`/`#agents` redirect về `#dossier` | ✅ (2026-06) |
| D-023 | **Transcript chat persist vào Supabase** (`web_chat`), bền qua restart; cache in-memory + degrade an toàn nếu chưa tạo bảng | ✅ (2026-06) |
| D-024 | **Output research bền ở `skill_runs`** (versioned), hiển thị 3 nơi; **tương tác được**: 👍/👎 (feed rating/học), Copy, Tạo lại (bản mới). Chat = nhẹ, giữ thoải mái | ✅ (2026-06) |

## Đã chốt các câu hỏi mở (2026-06)
Q-A→D-011 · Q-B→D-012 · Q-C→D-013 · Q-D→D-014.
| D-025 | Bỏ nhóm sidebar **② Chẩn đoán** (5 mục trùng); diagnosis truy cập qua **Hồ sơ doanh nghiệp**. Giữ **function A**: tên phân tích trong khối Chẩn đoán link sang trang chi tiết (biểu đồ + output inline) | ✅ (2026-06) |
| D-026 | **Đảo D-007: bỏ Max chat làm trung tâm.** Entry point = **Hồ sơ doanh nghiệp (form-first)** — user mới điền form hồ sơ rồi chạy chẩn đoán. Lý do: chat trống gây bí cho user mới. Backend chat.py giữ ngủ (gỡ sau) | ✅ (2026-06) |
| D-027 | **Tắt seed demo mặc định** (`WEB_SEED_DEMO`, mặc định off) → user mới thấy trạng thái SẠCH (0 cảnh báo/chiến dịch/jobs giả). Bật khi cần demo showcase | ✅ (2026-06) |
| D-028 | **Intake AI-adaptive**: Max phỏng vấn từng câu thông minh qua `run_discovery_turn` (giữ UI từng-câu). Wizard tĩnh 8 bước = fallback khi chưa có backend. ⚠️ **Đảo bởi D-032 (web):** luồng chính web đổi sang **wizard có bước** (để gắn chip gợi ý + skip + provenance per-câu); AI-hội-thoại để dành/bỏ sau | ✅ (2026-06) |
| D-029 | **SMART số liệu + roadmap 90 ngày chi tiết KHÔNG chốt ở Synthesis (M0)** — Synthesis chỉ ra **định hướng giai đoạn** (positioning, wedge, pillars, % phân bổ ngân sách, 0-30/31-60/61-90 = ưu tiên gì). SMART cụ thể (số, deadline, ngân sách thật) chỉ "đóng khung" khi tạo **1 Campaign Occasion cụ thể** (M1), kế thừa giai đoạn roadmap hiện tại. Lý do: Synthesis chưa có lever cụ thể (dịp/ngân sách/baseline) nên SMART ở đó là số đoán; bot Telegram đã làm đúng pattern này (`campaign_intake.build_campaign_draft_from_strategy` đọc `roadmap_90d[phase1]` + budget để pre-fill draft, KHÔNG tự tạo SMART rời). Web M1 occasion creation phải tái dùng pattern này. ⚠️ Cách thực thi đã điều chỉnh bởi **D-030** (ban đầu định không đổi prompt — band-aid gắn nhãn web; nay sửa thẳng prompt) | ✅ (2026-06) |
| D-041 | **Chọn phân khúc (wedge) trước khi làm T4+T5 — mirror bot (chưa code)** — Founder muốn *chọn nơi đánh*, không để AI tự quyết (bot tele đã có ở flow "8 câu": segment + USP-angle + channel + timeline — campaign_ideation.py:345). Web thêm bước **"Bạn muốn đánh vào (các) phân khúc nào?"** → feed **T4 Synthesis = chọn wedge** (phân khúc ưu tiên, khớp D-031 "tôn trọng wedge") + **T5 Playbook = viết đầy đủ cho phân khúc ưu tiên, gọn cho phụ**. ⚠️ Web hiện chạy 1-phát T1→T5 (non-interactive, D-015) vs bot interactive → **CHỐT option (b): web thành 2-PHASE có GATE.** Luồng: Intake → **Phase 1: T1-T3** (research, ra ICP + USP đề xuất) → **🚪 GATE** (founder chọn: USP giữ-nguyên/dùng-Max/refine + phân khúc muốn đánh=wedge, chọn trên data thật) → **Phase 2: T4 Synthesis (theo wedge) + T5 Playbook**. ⚠️ **Sửa D-015** (web không còn 1-phát — tách 2 phase có gate). Nút "Chạy phân tích toàn diện" → tách "Chạy nghiên cứu" + "Lập chiến lược". Gộp với D-039b (USP keep/refine cũng ở gate này). 📝 plan | 📝 plan |
| D-040 | **Chiến dịch 2-track (always-on + occasion) — mô hình M1 (chưa code)** — Sau Synthesis(T4)+Playbook(T5), nút tạo chiến dịch hiện CHỈ trỏ occasion (`#occasion`) → thiếu track always-on. Chốt mô hình (khớp D-017/018, Byron Sharp): **Synthesis = la bàn NẰM TRÊN cả 2 track**. (1) **Always-on** = hiện diện liên tục, baseline, nuôi bởi **Synthesis + Tactical Playbook (T5)** — KHÔNG chốt SMART (là nền). (2) **Occasion** = đợt theo dịp/sự kiện, kế thừa giai đoạn roadmap Synthesis, **CHỐT SMART/ngân sách/deadline ở đây** (D-029). Fix M1: nút sau synthesis rẽ **2 nhánh** ("Kích hoạt Always-on" từ T5 playbook · "Lập chiến dịch theo dịp" = occasion burst). T5 chính là xương của always-on → hiện T5 (D-038 A) cũng cấp nội dung always-on. M1 design — chưa build (#occasion mới là placeholder). 📝 plan | 📝 plan |
| D-039 | **Nav thiếu trên trang output + USP intake (chưa code)** — **(a) Nav bug (web-only):** các trang phân tích `P.market/customer/swot/pricing` KHÔNG có `actions` → thiếu nút **← back về Hồ sơ/trang chính**; sidebar/tab khó truy cập khi đọc output (navToggle ẩn). Fix: thêm back nhất quán MỌI trang output + đảm bảo sidebar/tab luôn truy cập được. **(b) USP intake gap → CHỐT option (b), mirror bot:** intake D-032 không có câu nhãn "USP"; nguyên liệu qua S3 `differentiation`. Sửa: câu differentiation thêm note *"chưa biết thì ghi 'chưa biết' — Max tự tìm"* (→ `usp_confidence=missing`); **quyết định giữ-nguyên/refine USP chuyển sang GATE phase 1.5 (D-041 option b)** — lúc đó Max ĐÃ đề xuất USP từ usp_definition → founder chọn "giữ định vị của tôi (`clear`) / dùng Max (`draft`) / refine" trên data thật. Pattern bot có sẵn (`usp_confidence` clear/draft/missing, handlers.py:2367). 📝 plan | 📝 plan |
| D-038 | **Synthesis sâu hơn (B, mạch tích hợp) + HIỆN Tactical Playbook (A) (chưa code)** — Founder thấy synthesis "không sâu bằng research" → gốc: synthesis cô đọng đúng thiết kế (la bàn, D-029/30) NHƯNG chiều sâu (T5) bị giấu trên web. **(A — hiện T5):** web đang SINH `tactical_playbook` (pipeline full) nhưng KHÔNG render (P.strategy chỉ hiện synthesis; ANALYSES thiếu tactical; "Tactical plays" ở SWOT là MOCK hardcode). Fix: hiển thị T5 thật (**TAB/TRANG RIÊNG "Tactical Playbook" — Founder chốt option (b)**) + **bỏ mock "Tactical plays"** (app.js:780). Content T5 đã xong (D-031 Segment→Phễu) — chỉ thiếu display. Web-only. **(B — tinh prompt `STRATEGY_SYNTHESIZER_SYSTEM`):** đổi từ 10-section liệt kê song song → **1 MẠCH LẬP LUẬN TÍCH HỢP** (cascade Playing to Win): 1 Luận điểm trung tâm → 2 Mạch nối research→lựa chọn (vì X market + Y competitor + Z customer → wedge W) → 3 Wedge (đánh đâu trước) → 4 Thắng bằng cách nào → **4b KHUNG ĐỊNH VỊ = USP (đinh + 3 biến thể) + SAVE**, đặt SAU mục 4 để là *kết tinh* không phải *áp đặt* (USP có thể nhân 1 dòng hook ở mục 1; SAVE CHỈ ở 4b, tránh lặp) → 5 định hướng giai đoạn (no SMART) → 6 đặt cược %(định hướng)+KPI theo dõi → 7 rủi ro+cờ đỏ. "Sâu" = LOGIC NỐI chứ không phải số section; ngắn hơn mà sâu hơn. Vẫn directional (D-029/30), trích TOWS (D-031 B1), trỏ chi tiết xuống T5. KHÔNG gộp T4+T5 (giữ kiến trúc la-bàn/bản-đồ). B đụng prompt engine (gom cùng D-035/036). 📝 plan | 📝 plan |
| D-037 | **2 bug hiển thị/sinh nội dung strategy + SWOT (chưa code)** — **(a) Strategy hiện synthesis 2 chỗ (web-only):** P.strategy (app.js:814-828) dùng `agentBar` ("AI Agent" + nút "Xem output"→modal) **+** card inline "Chiến lược 90 ngày" → cùng 1 bản synthesis vẽ 2 lần, user tưởng 2 output khác; lệch chuẩn các trang khác (dùng `agentSection` 1 card). Fix: gộp 1 card (bỏ agentBar/Xem-output, giữ inline collapsible + nút rerun/status). **(b) SWOT output cụt khi SINH (engine-side):** SWOT qua agentSection (doc-body KHÔNG max-height) vẫn cắt giữa câu ở Threat T4, chưa tới TOWS → **generation truncation** (đụng trần token/length), tái diễn (H&Y cũng cụt cuối TOWS). Fix: tăng `max_tokens` stage SWOT / ép SWOT cô đọng. Cần xác nhận độ dài content đã lưu. Khác bug bảng (D-034). 📝 plan | 📝 plan |
| D-036 | **Research stages (T1) bỏ "action plan" ở Strategic Implications — giữ insight, cắt roadmap (chưa code)** — Mỗi stage research tự đẻ kết luận chiến lược có ưu tiên/hành động: `COMPETITOR_SYSTEM` (prompts.py:374) "3 cơ hội xếp **Quick win/Medium-term/Long-term moat**"; Customer Insight ra "**5 Takeaway Actionable** (Quick win/Medium)". → Lấn & LẶP Synthesis (T4, việc duy nhất là tổng hợp); mỗi stage chạy MÙ nhau (market chưa thấy competitor/customer) → 5 kết luận một-phía dễ đá nhau; sai altitude (chẩn đoán ≠ lựa chọn — Roger Martin). Cùng anti-pattern với SMART-rải-khắp + TOWS-trùng. Fix: research close = **1-2 câu "so-what" mức INSIGHT** (phát hiện NGHĨA LÀ GÌ), KHÔNG xếp quick-win/medium/long/action — roadmap ưu tiên chỉ ở Synthesis(T4)+Tactical(T5). Market (prompts.py:252,257) đã nhẹ → giữ. Gom cùng D-035 khi đụng prompt engine. 📝 plan | 📝 plan |
| D-035 | **psychology_pricing (T2) cắt scope về nguyên lý + tách tactic B2B/B2C (chưa code)** — Chẩn đoán khi check output SpeeGo: stage T2 đang đẻ ra ĐỒ THỰC THI (ad headline/landing/social/sales script/post-purchase email/funnel TOFU-MOFU-BOFU+KPI) = sai tầng, trùng & đá Tactical Playbook (T5). **(A — ưu tiên)** Gốc: `MARKETING_PSYCHOLOGY_SYSTEM` (prompts.py:478-484) ra lệnh "Map nguyên tắc vào Headline&CTA / Landing / Social / Sales / Post-purchase" + dòng 446 "map vào funnel". Fix: đổi sang "chỉ ra đòn bẩy tâm lý nào HỢP + VÌ SAO (nối pain/insight), nhấn nguyên tắc ở touchpoint nào — mức NGUYÊN LÝ; KHÔNG viết copy/headline/script/email, KHÔNG dựng funnel+KPI (để T5)". Trước khi cắt: xác nhận T5 (Segment→Phễu — D-031) đã phủ copy/touchpoint → không mất nội dung. **(B — giữ on-demand, dùng khi cần)** `PRICING_STRATEGY_SYSTEM` Step 2-3 (prompts.py:520-537) hardcode tactic B2C/mass (charm 199k, installment "33k/ngày", "combo ưa chuộng", giả định "VN consumer") không phân biệt B2B/B2C/archetype/thị trường thật. Fix: phân nhánh theo loại hình+archetype+market từ profile (B2C/impulse→charm/installment/bundle; B2B/trust_building→số tròn/ROI-TCO/outcome/neo cost-of-inaction; bỏ giả định VN cứng). ⚠️ Engine dùng chung (bot sắp xóa). 📝 plan | 📝 plan |
| D-034 | **Sửa hiển thị output phân tích web (display-layer, WEB-ONLY — chưa code)** — Phát hiện khi chạy thật: (1) **BUG bảng:** `renderAIContent` (web/app.js:175) KHÔNG render bảng markdown `\|...\|` + `\|---\|` → competitor/SWOT/pricing hiện pipe + gạch thô, rất xấu. **Fix:** thêm parser bảng GFM (+ blockquote `>`) vào `renderAIContent`, dựng `<table class="tbl">` (CSS `.tbl` đã có). Web-only, KHÔNG đụng engine. ✅ **Vì `renderAIContent` dùng CHUNG cho mọi ô `.ai-output` → fix 1 lần áp dụng TẤT CẢ output có bảng** (market/competitor/customer/pricing/swot/synthesis/tactical), không sót trang nào. (2) **Card TAM/SAM/SOM + chart = số MOCK cứng** (2.400/480/24 tỷ VND, app.js:646-648,670) lệch output AI thật (USD) → **chọn cách (ii)**: web-side trích số có cấu trúc từ output market qua 1 LLM rẻ (nano/haiku, có cache) → render card; **thiếu → ẨN card** (không bao giờ hiện số sai). KHÔNG đụng prompt engine (option (i) bị loại vì lo rủi ro engine). (3) **Dọn mock sai ngành** còn lại trên trang phân tích (bullet "tăng trưởng 14%", "Benchmark ngành F&B" — sai cho mọi business không phải F&B). **Thứ tự:** #1 renderer bảng (ưu tiên — làm đẹp cả 5 trang 1 phát) → #2 card (ii) → #3 dọn mock. ✅ **#1 ĐÃ LÀM (đợt 1a + 1a-fix):** renderer block-aware (bảng/blockquote/list/hr/**code-fence ```**) + **bảng rộng CUỘN ngang** trong `.tbl-wrap` (max-width/min-width:0 chống tràn — sửa mobile cắt chữ + web không cuộn được) + `<pre>` cho ASCII. **#4 (mới, để sau):** **Positioning Map** đang là ASCII trong ```` ``` ```` → giờ hiện khối monospace gọn (cuộn được, hết vỡ/lòi backtick) nhưng CHƯA visual đẹp như bot — ✅ **ĐÃ port** (`enhancePosMaps`, visual 4 góc dark-theme + GÓC I viền sáng + ★ SẾP "Bạn ở đây"). **#5 (tối ưu sau):** parse ASCII regex dễ lệch → cho model xuất **positioning có cấu trúc** (2 trục + items + self) → render thẳng, bỏ ASCII (giống TAM/SAM/SOM ii, cần engine). + fix render `_nghiêng_` (markdown underscore italic, không đụng snake_case). 📝 plan | 📝 plan |
| D-033 | **Bỏ deploy GitHub Pages tĩnh — web chỉ chạy trên Railway (có backend)** — Founder đã chuyển hẳn sang Railway app. KHÔNG còn maintain bản static GitHub Pages (`index.html` bundle inline, dữ liệu mẫu, chip tĩnh). Hệ quả: (a) tính năng mới **mặc định coi như có backend** (API+Supabase+LLM) — không thiết kế 2 đường degrade nữa; (b) **bỏ bước build `webapp/build_standalone.py` + commit lại bundle** sau mỗi sửa; (c) **VẪN GIỮ** mức "không crash khi thiếu backend" cho **local dev** (`python run_web.py` chưa nối Supabase) — chỉ bỏ phần parity dữ liệu mẫu/chip cho GitHub Pages, không đập cơ chế degrade. ⏳ Dọn code static-demo thừa (root `index.html`, mock parity) = cleanup riêng, defer | ✅ (2026-06) |
| D-032 | **Intake web đa-level: gợi ý động + quyền bỏ qua → AI suy (WEB-ONLY)** — **GIỮ bộ câu hỏi ở tầng CMO** (JTBD / lựa-chọn-thay-thế / objection / khác biệt — sắc, không hạ độ cao); người không phải CMO dùng được nhờ **helper bình dân + chip gợi ý + quyền bỏ trống**. Mỗi câu khó: (1) **gợi ý động grounded** sinh theo ngành/sản phẩm/khách user đã nhập (chip để *nhận ra*, không bắt *nghĩ ra*); (2) **quyền bỏ qua** "chưa biết/chưa quyết" → đẩy cho AI suy; (3) **3 trạng thái nguồn gốc**: user tự trả lời **và** user chọn-gợi-ý = fact (dùng thẳng); **skip→AI suy = gắn nhãn "(giả định — cần kiểm chứng)"** trong output T1-T3 (chống bịa ngầm kiểu Zara); (4) **lõi bắt buộc nhỏ** (bán gì / cho ai / giá tầm / thách thức) + **thanh minh bạch** "Max đoán X/Y mục → phân tích mang tính giả định". Một luồng tự thích nghi 3 level (CMO gõ thẳng / owner chọn chip / người vội skip). **Phạm vi: chỉ web** (`web/`+`webapp/`+engine `agents/` mà web dùng); **KHÔNG đụng `bot/`** (telegram sắp xóa, chỉ là mẫu). Mở rộng D-028 (intake AI-adaptive web). ⚠️ Đính chính cấu trúc: codebase = 3 phần (web `web/`+`webapp/` · bot `bot/` sắp xóa · engine chung `agents/`/`frameworks/`/`tools/`/`storage/` = bộ não web). D1 archetype-fix trước nằm ở `bot/html_report.py` = bot-only, vô nghĩa với web (web không có banner archetype). 🔨 Chốt: wizard có bước = luồng CHÍNH (đảo D-028). **Đã build:** ✅ Step 1 (câu hỏi CMO-tier + skip→inferred + provenance + cột `intake_extra`, migration 012); ✅ Step 2 (chip gợi ý `/api/biz/intake/suggest` + render + chọn nhiều); ✅ Step 3 (câu chiến lược vào `to_context_string` + nhãn "(giả định)" cho inferred + thanh minh bạch trang strategy). ⚠️ Cần chạy migration 012 trên Supabase | ✅ (2026-06) |
| D-031 | **TOWS về đúng tầng + T5 Tactical đổi xương sống Segment→Phễu** — (1) **TOWS (SO/ST/WO/WT) ở đúng 1 nơi = cuối T3 SWOT** (`SWOT_SYSTEM`, prompts.py:1114) — đủ 4 ô, đây là cầu nối SWOT→chiến lược, không phải công cụ thực thi. (2) **T4 Synthesis trích dẫn TOWS để chọn wedge** (vd "chọn SO1 làm trục, song hành WT2 dựng moat"), KHÔNG lặp lại bảng TOWS. (3) **T5 `TACTICAL_PLAYBOOK_SYSTEM` BỎ SO/WO/WT làm cấu trúc per-segment** (strategy_prompts.py:125-174) → đổi xương sống thành **Segment → Phễu (TOFU/MOFU/BOFU)**; TOWS chỉ còn **tag 1 dòng** mỗi mũi ("phục vụ SO1"). Lý do: TOWS ở T5 = lỗi tầng (kéo abstraction chiến lược vào tầng thực thi) + trùng T3 + thiếu ST + ép horizon (SO=ngắn/WO=trung/WT=dài là bịa). (4) **đường giữa cho số**: 4a T5 test-param GIỮ cấu trúc thử + ngưỡng tương đối (CTR/ROAS) nhưng BỎ số tiền tuyệt đối ("8-12 triệu/tuần" → "ngân sách thử/đợt"); 4b T4 % phân bổ GIỮ nhưng gắn nhãn (định hướng) + diễn theo thứ tự ưu tiên. (5) **Fix bug archetype fallback generic** (banner ghi "Impulse/Gen Z/Zara" cho business đồ nhà plus-size nữ 25-45 = sai segment). ⚠️ `SWOT_SYSTEM`+`TACTICAL_PLAYBOOK_SYSTEM` dùng chung bot — như D-030, đây là sửa-cho-đúng cả 2 surface, không vi phạm tinh thần D-001. TOWS ở T3 (`must_have=True`) nên dù T5 bị skip vẫn còn đủ 4 nước | 📝 spec — chờ duyệt build |
| D-030 | **Synthesis thành "định hướng" SỬA TẠI GỐC (prompt), áp dụng cả bot + web** — Điều chỉnh cách thực thi của D-029. Thay vì band-aid gắn nhãn "(định hướng)" trên web, sửa thẳng `STRATEGY_SYNTHESIZER_SYSTEM` (`agents/prompts.py:618`, dùng chung bot+web qua `run_targeted_pipeline`): section 4 *"SMART Goals (số cứng)"* → *"Mục tiêu định hướng theo giai đoạn"* (định tính, không số cam kết); section 7 KPI *"Targets mỗi giai đoạn"* → *"KPI cần theo dõi"* (đo cái gì, không chốt số bao nhiêu). SMART số thật để dành Occasion (M1). **KHÔNG hỏng pre-fill campaign của bot** vì pre-fill đọc từ JSON strategy đường khác (`CMO_STRATEGY_SYSTEM.roadmap_90d[].smart_goals`), không phải từ Markdown synthesis này. ⚠️ Đính chính: note CODEMAP cũ ghi `STRATEGY_SYNTHESIZER_SYSTEM` = web-only là **SAI** — nó dùng chung bot (`bot/handlers.py:5845`) + web (`webapp/business.py:589`). Lập luận marketing (la bàn không cần tọa độ chính xác) đúng cho cả 2 surface → đây là sửa-cho-đúng, không vi phạm tinh thần D-001 | ✅ (2026-06) |

## Tiến độ build web-only (2026-06)
- ✅ **Đợt 1a** (`c224647`,`0a84007`,`8c58d29`,`2cf012b`): D-034#1 renderer bảng/blockquote/list/code-fence + bảng cuộn ngang (mobile+web) · D-034#4 Positioning Map visual (port bot) + polish · D-037a strategy gộp 1 card · D-032§11 intake textarea/Enter/chip-multi-line/bỏ-placeholder · D-039b note differentiation · bỏ rail phải · render `_nghiêng_`.
- ✅ **Đợt 1b** (`11a990c`): D-034#3 dọn mock F&B 5 trang phân tích · D-039a nav back "← Hồ sơ" · D-038A tab Tactical Playbook (hiện T5 thật).
- ⏳ **Còn web-only:** D-034#2 (TAM/SAM/SOM số thật qua LLM — cần endpoint).
- ⏳ **Đợt engine (gom):** D-035 (psychology scope) · D-036 (research implications) · D-037b (SWOT truncation) · D-038B (synthesis mạch tích hợp + 4b USP/SAVE) · D-034#5 (pos-map structured).
- ⏳ **Flow lớn:** D-041 (web 2-phase gate) · D-039a-sidebar mobile · D-040 (campaign 2-track, M1).
- ✅ **Đợt engine** (prompt): D-038B synthesis mạch tích hợp (cascade + 4b USP/SAVE) · D-035A psychology cắt scope T2 · D-035B pricing tách B2B/B2C · D-036 research bỏ roadmap (giữ insight) · D-037b SWOT 22k token + ép cô đọng (hết cụt TOWS). ⏳ Nghiệm thu = chạy lại pipeline thật.
- ⏳ Còn lại: D-034#2 (TAM/SAM/SOM card số thật) · D-034#5 (pos-map structured) · D-041 (web 2-phase gate) · D-040 (campaign 2-track M1).
- ✅ **D-034 #2** (TAM/SAM/SOM card số thật, web trích từ output) + **D-041** (web 2-phase có GATE: research T1-T3 → chọn wedge+USP trên data thật → strategize T4-T5; wedge inject vào context, synthesis tôn trọng). Đổi nút chính → "Chạy nghiên cứu". Sửa D-015.
- ⏳ Còn lại: D-034#5 (pos-map structured) · D-040 (campaign 2-track M1).
- ✅ **D-040** (campaign 2-tuyến entry + logic): spec M1-campaign-2track.md (Byron Sharp + Binet&Field, industry-aware 15 ngành); endpoint campaign_plan sinh content pillars (always-on) + gợi ý occasion theo ngành; hub UI 2 thẻ. Occasion creation đầy đủ (SMART) = M1 lớn, để sau.
- ✅ **D-034 #5** (pos-map structured): model xuất JSON posmap; enhancePosMaps ưu tiên JSON → render thẳng, fallback ASCII.
- ✅ Nợ kỹ thuật #1: nút full bỏ qua gate (Tactical empty-state) → trỏ #strategy.
- ⏳ Còn: luồng tạo Occasion đầy đủ (M1 lớn) · dọn code chết nhẹ · GitHub Pages/bot cleanup.
- 📝 **D-042 (nợ dọn dẹp — defer, làm khi rảnh):**
  - **Code chết web/app.js:** `SKILL_TO_TASK` (không index ở đâu), `swotCell` (đã gỡ khỏi SWOT), `ph` trong INTAKE_STEPS (textarea bỏ qua placeholder).
  - **Mock thừa web/data.js:** `M.personas`, `M.pricingTiers`, `M.competitors` (đã gỡ card ở D-034#3) → không còn dùng. Giữ `M.saveFramework`/`M.directionalGoals` (strategyMock fallback no-backend còn dùng).
  - **Tàn dư GitHub Pages (D-033):** root `index.html` bundle inline + `webapp/build_standalone.py` parity — không maintain nữa, xoá khi tiện. `bot/` xoá khi bỏ tele (D1 archetype-fix trong đó moot với web).
  - Nguyên tắc: dọn theo từng đợt nhỏ, có test chặn; KHÔNG đập `strategyMock`/degrade-no-backend (còn dùng cho local dev).
- 📝 **D-043 (spec) — M1 Luồng tạo Occasion đầy đủ** (`slices/M1-occasion-creation.md`): occasion = activation spike có window, chốt SMART thật (vì đủ lever: dịp/window/ngân sách/baseline). Arc 5 pha (teaser→build→peak→last-call→after). Kế thừa Synthesis(USP/wedge/roadmap phase)+Playbook+ngành; TÁI DÙNG pattern bot `campaign_intake.build_campaign_draft_from_strategy` (pre-fill SMART từ roadmap, D-029). Luồng: chọn dịp(industry-aware)→lever→Max sinh brief SMART→review→lưu campaigns_v2→(lịch M1.2). Phân kỳ M1.1 (chốt SMART) / M1.2 (lịch 2-track) / M1.3 (always-on activation).
  - ✅ **Founder quyết (2026-06-21):** kế thừa = pre-fill (KHÔNG lock), founder đè được mọi field + SMART số là của founder; lan can nhất quán chỉ NHẮC không chặn. Baseline 'chưa rõ' → SMART để khoảng + nhãn (ước tính), không chặn. Sinh brief = web-side 1 LLM call (KHÔNG kéo dependency bot).
  - ✅ **M1.1 BUILT (2026-06-21):** `business.occasion_draft`/`save_occasion` (OPS_BRIEF, cache, degrade {}) + route `/api/biz/occasion`(+`/save`) + FE wizard (`openOccasionWizard` → chọn dịp/lever → sinh brief Markdown → review → lưu). Lưu = skill_run `occasion_brief` + record `campaigns` (không migration). Test `test_occasion_m1`. M1.2 (lịch 2-track) chờ duyệt.
- ✅ **D-044 — Mục đích đợt = trục WHY (tag, KHÔNG phải tuyến mới)** (2026-06-21): Founder hỏi "có nên có camp theo mục đích (brand/retention…)?". Quyết: occasion(WHEN) và mục đích(WHY) là **2 trục khác** — mỗi campaign nằm ở cả 2. KHÔNG thêm tuyến thứ 3. **brand** = đã là việc của always-on (đừng trùng); **acquisition/conversion** = đã có. → thêm **tag "mục đích đợt"** (acquisition/conversion/brand/retention) định hình brief occasion. **BUILT:** `OCCASION_OBJECTIVES` (4 mục đích) shape prompt + lưu `primary_goal`; FE selector `OCC_OBJECTIVES`/`occ-obj`. Test mở rộng `test_occasion_m1`.
- 📝 **D-045 (spec) — M2 Retention/Lifecycle** (`slices/M2-retention-lifecycle.md`): retention/winback là khoảng trống THẬT (behavior-triggered, không phải lịch/dịp) → tách MODULE riêng nhưng cùng la bàn Synthesis. TÁI DÙNG engine skills `retention_strategy`(3 giai đoạn)+`winback_campaign`(3 bước) đã có. 2 mức: A=strategy-only (làm ngay, không cần data — MVP M2.1), B=data-driven RFM (cần order data — sau). Phân kỳ M2.1(playbook)/M2.2(RFM)/M2.3(automation).
  - ✅ **Founder chốt (2026-06-21):** M2.1 = **cẩm nang if-then, KHÔNG cần order data.** Tách 2 việc: "biết phải làm gì mỗi tình huống" (không cần data → Max sinh playbook: dấu hiệu nhận biết→hành động→kênh→tin mẫu, founder áp tay) vs "tự phát hiện khách nào ở tình huống nào" (cần RFM → để Mức B). Threshold thời gian = ước tính theo chu kỳ ngành + nhãn; gửi tin thủ công ở M2.1.
  - ✅ **M2.1 BUILT (2026-06-21):** `business.retention_draft`/`save_retention` (2 mode retention/winback, OPS_BRIEF, cache, degrade {}) + route `/api/biz/retention`(+`/save`) + FE `openLifecycleWizard` (2 nút trong hub Lập chiến dịch → lever nhẹ → cẩm nang bảng if-then → lưu). Prompt RÀNG BUỘC "founder không có data, đưa cẩm nang tự nhìn ra & áp tay"; ngưỡng = ước tính theo ngành + nhãn. Lưu = skill_run `retention_playbook`/`winback_playbook` + record `campaigns`. Test `test_retention_m2`. M2.2 (RFM data-driven) để sau.
- ✅ **D-046 — M1.2 Lịch nội dung 2-track nối THẬT** (`slices/M1.2-calendar-2track.md`, 2026-06-21): khép vòng giá trị — campaign occasion vừa tạo (M1.1) giờ hiện trên lịch + sinh nội dung slot thật. **BUILT:** `business.calendar_plan` (anchor=thứ Hai tuần này, map occasion window→tuần qua `_week_of`, always-on từ pillars D-040, retention không window→không lên lịch; degrade {}) + `gen_calendar_post` (OPS_CONTENT_CREATIVE, bám pillar/brief→skill_run `calendar_post`) + route `/api/biz/calendar`(+`/gen`). FE: `loadRealCalendar`→`calPlan()` ưu tiên thật (fallback mock + badge), nút "Tạo campaign theo dịp"→`openOccasionWizard` (bỏ prompt() prototype), `cal-gen`→sinh bài thật→modal. Test `test_calendar_m12`. NGOÀI: kéo-thả/.ics/auto-đăng. Còn lại: D-042 (dọn debt) → M2.2 (RFM).
